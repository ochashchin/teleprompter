package com.example.kotlinmultiplatform.frame

/**
 * Retained as an empty placeholder so any references that survived refactoring
 * still compile.  The capture pipeline has been removed — Android PiP renders
 * the Activity window (Compose PlayerScreen) directly without pixel copying.
 */
class FrameCaptureState {
    fun clear() = Unit
}
